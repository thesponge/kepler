import { libravatarSrc } from '../../../helpers/libravatar-src';
import { module, test } from 'qunit';

module('Unit | Helper | libravatar src');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = libravatarSrc(42);
  assert.ok(result);
});
